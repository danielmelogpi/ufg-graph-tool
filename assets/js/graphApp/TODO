GraphApp.Handler.DragNode
	@TODO Otimizar a parte do updateEdges, que deixa o dragging do note um pouco lento
	provavelmente por causa dos forEach. Firefox is more sensible on this.

 GraphApp.Handler.DragEdge
	this.curveToMousePosition	@TODO é necessário fazer o calculo de animação. 
			
GraphApp.Control.NodeDraw
	this.createNode /** @TODO 	Corrigir problemas quando o scale não é 1 */
	// atualizar coordenadas de outros selecionados quando arrastar
	//quando o controle não é navegação deve prevenir o drawn

GraphApp.FollowLine
	this.updateToMouse /** @TODO the diference in position relatively to the mouse needs improvement */

GraphAPP.Graph
	Checagem de instancias dos objetos das formas. 
	Os objetos passados para adição no grafo tem a instancia correta?

GraphApp.Stage
	this.addSelectionMark	// @TODO fork the layers

GraphApp.SelectedMark
	createMarkShape	//@TODO correct style properly
	// rectangular forms appear to be in front of stuff


//add color chooser
GraphApp.FormPanel
this.events = {
		afterDraw :

// não deve ser permitido selecionar uma aresta e um vértice e ainda ter um painel
GraphApp.FormPanel



Adicionar tooltips nos diversos controles da página
Adicionar colorpicker nos paineis